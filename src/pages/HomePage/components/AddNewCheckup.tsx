interface Props {
    closeModal: VoidFunction;
}

const AddNewCheckup = ({}: Props) => {
    return (
        <div>
            <h1>Hola from modal form checkup</h1>
        </div>
    );
};

export default AddNewCheckup;
